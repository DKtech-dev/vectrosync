{
  "name": "catenary-enterprise-industrial-twin",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "npm ci --ignore-scripts --cache frontend/.npm-cache --prefix frontend && npm run build --prefix frontend"
  }
}
